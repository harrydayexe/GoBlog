{{define "header"}}
<header class="sticky top-0 z-50 bg-white border-b border-gray-200 shadow-sm">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            <!-- Site Title -->
            <div class="flex-shrink-0">
                <a href="/" class="text-2xl font-bold text-gray-900 hover:text-blue-600 transition-colors">
                    {{.SiteTitle}}
                </a>
            </div>

            <!-- Navigation -->
            <nav class="flex space-x-8">
                <a href="{{.BlogRoot}}" class="text-gray-700 hover:text-blue-600 font-medium transition-colors">
                    Home
                </a>
                {{if .TagsEnabled}}<a href="{{.BlogRoot}}tags" class="text-gray-700 hover:text-blue-600 font-medium transition-colors">
                    Tags
                </a>{{end}}
                {{- if .SeriesEnabled}}
                <a href="{{.BlogRoot}}series" class="text-gray-700 hover:text-blue-600 font-medium transition-colors">
                    Series
                </a>{{end}}
                {{if .FeedsEnabled}}<a href="{{.BlogRoot}}rss.xml" class="text-gray-700 hover:text-blue-600 font-medium transition-colors" title="RSS Feed">
                    RSS
                </a>{{end}}
            </nav>
        </div>
    </div>
</header>
{{end}}
